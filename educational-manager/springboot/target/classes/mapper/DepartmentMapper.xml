<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.DepartmentMapper">

    <sql id="Base_Column_List">
        id,img,name,description,student_id
    </sql>

<!--    <select id="selectAll" resultType="com.example.entity.Department">-->
    <select id="selectAll" resultMap="DepartmentWithStudentNameResultMap">
    select department.*, student.name as studentName
        from department
        left join student on department.student_id = student.id
        <where>
            <if test="id != null"> and id= #{id}</if>
            <if test="name != null"> and name like concat('%', # {name}, '%')</if>
            <if test="img != null"> and img= #{img}</if>
            <if test="description != null"> and description like concat('%', #{description}, '%')</if>
            <if test="studentId != null"> and student_id= #{studentId}</if>
        </where>
        order by id desc
    </select>
    <resultMap id="DepartmentWithStudentNameResultMap" type="com.example.entity.Department">
        <id property="id" column="id" />
        <result property="img" column="img" />
        <result property="name" column="name" />
        <result property="description" column="description" />
        <result property="studentId" column="student_id" />
        <result property="studentName" column="studentName" />
    </resultMap>

    <select id="selectById" resultType="com.example.entity.Department">
        select
        <include refid="Base_Column_List" />
        from department
        where id = #{id}
    </select>

    <delete id="deleteById">
        delete from department
        where  id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.Department" useGeneratedKeys="true">
        insert into department
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="name != null">name,</if>
            <if test="img != null">img,</if>
            <if test="description != null">description,</if>
            <if test="studentId != null">student_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="name != null">#{name},</if>
            <if test="img != null">#{img},</if>
            <if test="description != null">#{description},</if>
            <if test="studentId != null">#{studentId},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.Department">
        update department
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="img != null">
                img = #{img},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="studentId != null">
                student_id = #{studentId},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>